# 📝 Handover: Состояние проекта Planner (Заморозка)

**Дата фиксации:** 11.05.2026
**Статус:** Golden Build (67/67 tests passed). Полностью развернуто на VPS.

## 🏗 Архитектура и "Мозги"
1. **AI Vision (Бот):** 
   - Используем **Llama 4 Scout** (`meta-llama/llama-4-scout-17b-16e-instruct`) через Groq.
   - **Важно:** Бот сжимает фото через Pillow (ширина 2000px, JPEG 85%) перед отправкой. Это обходит лимиты Groq API.
   - **Логика трат:** Траты в базу идут со знаком `+`, Доходы со знаком `-`.
   - **Год:** В промпте жестко зашит 2026 год.

2. **Карьерный капитал:**
   - Таблица `tasks` расширена полями: `impact_notes` (текст), `is_milestone` (bool), `estimated_minutes`, `actual_minutes`.
   - Настроены авто-миграции в `app/db/database.py`.

3. **Frontend:**
   - Построен на **HTMX**. Все кнопки (выполнить/удалить) работают без перезагрузки страницы.
   - Если что-то не обновляется — проверь `HX-Trigger` в ответах FastAPI.

## 🖥 Сервер и Деплой
- **IP:** 91.186.217.66
- **Путь:** `/opt/projects/planner/`
- **Команды:**
  - Обновить: `git pull origin main && docker compose down && docker compose up -d --build`
  - Логи бота: `docker logs -f telegram_bot`
  - Доступ в БД: `sqlite3 planner.db`

## 🎯 Что пилить следующим шагом?
1. **Экспорт достижений:** Нужна кнопка "Скачать отчет" (генерация Markdown или PDF из всех `is_milestone=True`).
2. **Финансы:** Добавить Chart.js для отрисовки круговой диаграммы расходов в `stats.html`.
3. **Бот:** Реализовать напоминания через APScheduler (заготовки в `app/services/recurring_service.py`).

**Проект в идеальном порядке. Приятного отдыха!**
